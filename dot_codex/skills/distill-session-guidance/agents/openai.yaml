interface:
  display_name: "Distill Session Guidance"
  short_description: "Capture steering and distill durable guidance"
  default_prompt: "Use $distill-session-guidance to capture reusable steering, reload pending thread guidance, or explicitly flush verified lessons."

policy:
  allow_implicit_invocation: false
